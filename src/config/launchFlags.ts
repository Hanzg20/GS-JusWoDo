// Payments/escrow are deferred to v2 — launch model is browse + in-app
// chat, deals close off-platform (matching yellowducky.ca). Flip this back
// on once Stripe collection + provider payouts are ready to go live.
export const PAYMENTS_ENABLED = false;
